import { AlertTemplate, InformationTemplate, ListTemplate } from 'react-native-carplay'

const LoginTemplate = new ListTemplate({
	title: 'Please login on iPhone',
	items: [],
})

export default LoginTemplate
